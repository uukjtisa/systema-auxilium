# ============================================================================
# SYSTEMA AUXILIUM — REQUIREMENTS
# ----------------------------------------------------------------------------
# Basic install:   pip install -r requirements.txt
# Recommended:     run  python setup.py  — it builds a .venv and installs these
#                  for you, with an interactive optional-modules picker.
# ============================================================================

# ── Core / GUI ────────────────────────────────────────────────────────────
PyQt6                 # Desktop GUI framework
Pillow                # Image handling (thumbnails, tray icon, shortcut icons)
pystray               # System-tray icon
psutil                # System information
send2trash            # Safe file deletion (Recycle Bin / Trash)
pyautogui             # Screen capture + desktop automation
requests              # HTTP for provider scripts & skills
markdown2             # Markdown rendering in chat
matplotlib            # LaTeX / plot rendering
pyparsing             # Markdown / table parsing helper
updater-gitplucker>=0.6.0  # Self-update from GitHub (Settings > System/General > Check for Updates)

# ── AI Provider SDKs (used by the bundled provider scripts) ─────────────────
anthropic             # Anthropic Claude API
google-genai          # Google Gemini API (unified SDK; replaces google-generativeai)
openai                # OpenAI-compatible REST providers

# ── Memory recall (RAG embeddings) ──────────────────────────────────────────
fastembed
numpy
# onnxruntime 1.20+ has DLL initialization issues on Windows.
# Pin to 1.19.2 for stability. Do NOT upgrade without testing.
onnxruntime==1.19.2

# ── Voice input / output ────────────────────────────────────────────────────
sounddevice
webrtcvad-wheels      # prebuilt wheels for VAD (no C compiler needed)
SpeechRecognition
edge-tts
pygame                # audio playback

# ============================================================================
# OPTIONAL — enable per feature (installed by default unless commented out)
# ============================================================================

# --- web-browser skill (recommended) ---
beautifulsoup4
duckduckgo_search
trafilatura

# --- data-viz skill (recommended) ---
pandas

# --- Offline TTS ---              (uncomment to enable)
# pyttsx3

# --- Offline STT (Vosk) ---       (uncomment to enable)
# vosk

# --- Local LLaMA models ---       (capable hardware only)
# llama-cpp-python
#   Windows CUDA wheels:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121

# --- Silero VAD (ML voice detection) ---
#   Install torch SEPARATELY — do NOT add it here (conflicts with onnxruntime on Windows):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# torch
# torchaudio

# ============================================================================
# PLATFORM NOTES
# ============================================================================
# Windows:
#   - Visual C++ Redistributable 2015–2022 (x64):
#     https://aka.ms/vs/17/release/vc_redist.x64.exe
#   - Keep onnxruntime==1.19.2 (1.20+ causes DLL init failures).
#
# Linux (Debian/Ubuntu):
#   sudo apt-get install python3-pyqt6 python3-tk portaudio19-dev
#   - The "Run as administrator" desktop-shortcut option uses `pkexec`
#     (package: policykit-1).
#
# macOS:
#   brew install portaudio
#   - Grant Accessibility permission in System Settings → Privacy & Security.
# ============================================================================
