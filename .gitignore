# Maintainer notes stay in the working dir only — never in the repo.
.dev-copy
.dev/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Runtime counters written by the app (and by any local test run). The rest of
# data/ is user state that is never mirrored; these two kept getting staged by
# accident after running the suite inside the repo copy.
data/token_usage.json
data/token_usage_output.json
